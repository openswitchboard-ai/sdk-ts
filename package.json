{
  "name": "@openswitchboard/sdk",
  "version": "0.12.0",
  "description": "TypeScript SDK for the OpenSwitchboard protocol - typed listings, validators, safe builders, and the no-leak redaction proof.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/openswitchboard-ai/sdk-ts.git"
  },
  "homepage": "https://openswitchboard.ai",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@openswitchboard/schema": "file:../schema",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
